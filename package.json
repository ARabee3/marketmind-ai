{
  "name": "marketmind-ai",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "check": "npm run build -w @marketmind/api && npm run test -w @marketmind/api && npm run test:e2e -w @marketmind/api && npm run check:ai",
    "check:ai": "cd services/ai && uv run pytest"
  },
  "overrides": {
    "multer": "2.2.0"
  }
}
