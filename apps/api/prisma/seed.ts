/**
 * Prisma seed script.
 *
 * Seeds initial roles and permissions for the RBAC system.
 * Implementation will be added when Auth/RBAC modules are built.
 *
 * Run with: npm run prisma:seed
 */

async function main() {
  console.log('🌱 Seed script placeholder — add seed data when models are defined.');
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  });
