import { Module } from "@nestjs/common";

/**
 * UsersModule — placeholder for Sprint 1.
 *
 * Will contain:
 * - UsersService: user lookup and persistence.
 * - UsersRepository: Prisma-based user data access.
 *
 * Implementation is part of Auth work (Issue #5).
 */
@Module({})
export class UsersModule {}
